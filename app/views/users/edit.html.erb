<div class="container">
  <div class="row">
  	<div class="col-md-6 offset-md-3">
  		<br><br><br>
				<%= form_with model: @user, local: true,  html: { class: "form-group" } do |f| %>
  				<h1 class="h3 mb-3 font-weight-normal text-center">Editer vos informations</h1>
  
  					<div class="form-group">
    					<p> Nom de famille :</br> <%= f.text_field :first_name, class: "form-control"%> </p>
						</div>

						<div class="form-group">
    					<p> Prenom :</br> <%= f.text_field:last_name, class: "form-control"%> </p>
						</div>

    				<div class="actions mt-5">
     		 			<%= f.submit "Mise à jour", class: "btn btn-lg btn-primary btn-block" %>
    				</div>

     		<%end%>

 		</div>
	</div>
</div>

