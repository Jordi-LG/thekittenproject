<%= render 'jumbotron' %>

<div class = "container">
  <div class="card-columns">
    <% @items.each do |item| %>
        <img src="<%= item.image_url %>" alt="..." class="card-img-top p-2">
    <% end %>
  </div>
</div>
