<%= render 'jumbotron' %>

<div class="bgi">
<div class = "container mt-4">
  <div class="card-columns">
    <% @items.each do |item| %>
    <div class="">
    <div class="cardt mb-2">
      <%= link_to item_path(item.id) do%>
        <img src="<%= item.image_url %>" alt="..." class="card-img-top">
      <% end %>
      </div>
    <% end %>
  </div>
  </div>
</div>
</div>
